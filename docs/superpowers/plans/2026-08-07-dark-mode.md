# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dark mode no frontend seguindo `prefers-color-scheme` por padrão, com toggle manual na sidebar persistido em `localStorage`.

**Architecture:** Bloco `:root[data-theme="dark"]` redefine as CSS variables existentes em `styles.css`. Um `ThemeProvider` aplica `data-theme` no `<html>`, com lógica pura em `src/theme/theme.ts` (testável sem DOM). Gráficos Recharts leem cores computadas via hook `useThemeColors()` para que o CSS continue sendo a única fonte de verdade.

**Tech Stack:** React 19, Vite, Vitest, Recharts. Sem dependências novas.

**Spec:** `docs/superpowers/specs/2026-08-07-dark-mode-design.md`

**Todos os comandos rodam em `frontend/`.**

---

### Task 1: Lógica pura de tema (`theme.ts`)

**Files:**
- Create: `frontend/src/theme/theme.ts`
- Test: `frontend/src/theme/theme.test.ts`

- [ ] **Step 1: Write the failing test**

Criar `frontend/src/theme/theme.test.ts`:

```ts
import { describe, expect, it } from "vitest";

import { nextMode, parseMode, resolveTheme } from "./theme";

describe("resolveTheme", () => {
  it("system segue a preferência do SO", () => {
    expect(resolveTheme("system", true)).toBe("dark");
    expect(resolveTheme("system", false)).toBe("light");
  });
  it("modo manual ignora a preferência do SO", () => {
    expect(resolveTheme("light", true)).toBe("light");
    expect(resolveTheme("dark", false)).toBe("dark");
    expect(resolveTheme("light", false)).toBe("light");
    expect(resolveTheme("dark", true)).toBe("dark");
  });
});

describe("nextMode", () => {
  it("cicla system → light → dark → system", () => {
    expect(nextMode("system")).toBe("light");
    expect(nextMode("light")).toBe("dark");
    expect(nextMode("dark")).toBe("system");
  });
});

describe("parseMode", () => {
  it("aceita valores válidos", () => {
    expect(parseMode("light")).toBe("light");
    expect(parseMode("dark")).toBe("dark");
    expect(parseMode("system")).toBe("system");
  });
  it("trata inválido/ausente como system", () => {
    expect(parseMode(null)).toBe("system");
    expect(parseMode("blue")).toBe("system");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- src/theme/theme.test.ts`
Expected: FAIL — `Cannot find module './theme'` (ou equivalente).

- [ ] **Step 3: Write minimal implementation**

Criar `frontend/src/theme/theme.ts`:

```ts
export type ThemeMode = "system" | "light" | "dark";
export type ResolvedTheme = "light" | "dark";

export function resolveTheme(mode: ThemeMode, systemPrefersDark: boolean): ResolvedTheme {
  if (mode === "system") return systemPrefersDark ? "dark" : "light";
  return mode;
}

export function nextMode(mode: ThemeMode): ThemeMode {
  if (mode === "system") return "light";
  if (mode === "light") return "dark";
  return "system";
}

export function parseMode(value: string | null): ThemeMode {
  return value === "light" || value === "dark" ? value : "system";
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- src/theme/theme.test.ts`
Expected: PASS (7 testes).

- [ ] **Step 5: Commit**

```bash
git add frontend/src/theme/theme.ts frontend/src/theme/theme.test.ts
git commit -m "feat(ui): add pure theme resolution logic"
```

---

### Task 2: Paleta escura no CSS

**Files:**
- Modify: `frontend/src/styles.css`

- [ ] **Step 1: Adicionar `color-scheme` e as variables novas ao tema claro**

Em `frontend/src/styles.css`, substituir o bloco `:root` atual (linhas 1–16) por:

```css
:root {
  color-scheme: light;
  --surface: #fcfcfb;
  --page: #f9f9f7;
  --ink: #0b0b0b;
  --ink-2: #52514e;
  --muted: #898781;
  --grid: #e1e0d9;
  --baseline: #c3c2b7;
  --border: rgba(11, 11, 11, 0.1);
  --blue: #2a78d6;
  --blue-dark: #1c5cab;
  --blue-100: #cde2fb;
  --red: #e34948;
  --good: #006300;
  --critical: #d03b3b;
  --input-bg: #fff;
  --warn-bg: #fffaf7;
  --warn-border: #e8b8a8;
}

:root[data-theme="dark"] {
  color-scheme: dark;
  --surface: #1a1a18;
  --page: #121210;
  --ink: #f0efec;
  --ink-2: #b3b1aa;
  --muted: #8b897f;
  --grid: #32312c;
  --baseline: #4a4941;
  --border: rgba(240, 239, 236, 0.12);
  --blue: #5c9fe8;
  --blue-dark: #82b6ee;
  --blue-100: #1d3a5c;
  --red: #ef6d6c;
  --good: #5dbb5d;
  --critical: #ef6d6c;
  --input-bg: #232320;
  --warn-bg: #2a201b;
  --warn-border: #6b4a3a;
}
```

- [ ] **Step 2: Trocar os usos de cor fixa por variables**

No mesmo arquivo, no bloco `select, input` trocar `background: #fff;` por `background: var(--input-bg);` e adicionar `color: var(--ink);`. Resultado:

```css
select, input {
  font: inherit;
  padding: 6px 8px;
  border-radius: 8px;
  border: 1px solid var(--baseline);
  background: var(--input-bg);
  color: var(--ink);
}
```

Adicionar após o bloco `.card` (linha ~47) a variante de alerta:

```css
.card.warn { border-color: var(--warn-border); background: var(--warn-bg); }
```

- [ ] **Step 3: Verificação estática**

Run: `npm run build`
Expected: build passa sem erros.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/styles.css
git commit -m "feat(ui): add dark palette via data-theme CSS variables"
```

---

### Task 3: ThemeProvider + useThemeColors

**Files:**
- Create: `frontend/src/theme/ThemeContext.tsx`
- Modify: `frontend/src/main.tsx`
- Modify: `frontend/index.html`

- [ ] **Step 1: Criar `frontend/src/theme/ThemeContext.tsx`**

```tsx
import {
  createContext,
  useCallback,
  useContext,
  useEffect,
  useMemo,
  useState,
  type ReactNode,
} from "react";

import { nextMode, parseMode, resolveTheme, type ResolvedTheme, type ThemeMode } from "./theme";

const STORAGE_KEY = "theme";

interface ThemeContextValue {
  mode: ThemeMode;
  resolved: ResolvedTheme;
  cycle: () => void;
}

const ThemeContext = createContext<ThemeContextValue | null>(null);

function readStoredMode(): ThemeMode {
  try {
    return parseMode(localStorage.getItem(STORAGE_KEY));
  } catch {
    return "system";
  }
}

function storeMode(mode: ThemeMode) {
  try {
    if (mode === "system") localStorage.removeItem(STORAGE_KEY);
    else localStorage.setItem(STORAGE_KEY, mode);
  } catch {
    // localStorage indisponível — tema só não persiste
  }
}

const darkQuery = () => window.matchMedia("(prefers-color-scheme: dark)");

export function ThemeProvider({ children }: { children: ReactNode }) {
  const [mode, setMode] = useState<ThemeMode>(readStoredMode);
  const [systemDark, setSystemDark] = useState(() => darkQuery().matches);

  useEffect(() => {
    const mq = darkQuery();
    const onChange = (e: MediaQueryListEvent) => setSystemDark(e.matches);
    mq.addEventListener("change", onChange);
    return () => mq.removeEventListener("change", onChange);
  }, []);

  const resolved = resolveTheme(mode, systemDark);

  useEffect(() => {
    document.documentElement.dataset.theme = resolved;
  }, [resolved]);

  const cycle = useCallback(() => {
    setMode((m) => {
      const next = nextMode(m);
      storeMode(next);
      return next;
    });
  }, []);

  const value = useMemo(() => ({ mode, resolved, cycle }), [mode, resolved, cycle]);
  return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;
}

export function useTheme(): ThemeContextValue {
  const ctx = useContext(ThemeContext);
  if (!ctx) throw new Error("useTheme requer ThemeProvider");
  return ctx;
}

export interface ThemeColors {
  muted: string;
  baseline: string;
  blue: string;
  blueDark: string;
  red: string;
  ink2: string;
}

export function useThemeColors(): ThemeColors {
  const { resolved } = useTheme();
  return useMemo(() => {
    const style = getComputedStyle(document.documentElement);
    const v = (name: string) => style.getPropertyValue(name).trim();
    return {
      muted: v("--muted"),
      baseline: v("--baseline"),
      blue: v("--blue"),
      blueDark: v("--blue-dark"),
      red: v("--red"),
      ink2: v("--ink-2"),
    };
    // resolved na dependência: recalcular quando o tema efetivo muda
  }, [resolved]);
}
```

Nota: `useThemeColors` roda depois do effect que seta `data-theme` (o effect do provider roda antes da renderização dos filhos consumidores no mesmo commit; na prática o valor lido corresponde ao tema atual porque o `data-theme` inicial já foi aplicado pelo script anti-flash do `index.html`, e mudanças subsequentes passam pelo effect antes do re-render dos consumidores via mudança de `resolved`).

- [ ] **Step 2: Envolver a app em `frontend/src/main.tsx`**

Substituir o conteúdo por:

```tsx
import React from "react";
import ReactDOM from "react-dom/client";

import App from "./App";
import { ThemeProvider } from "./theme/ThemeContext";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ThemeProvider>
      <App />
    </ThemeProvider>
  </React.StrictMode>
);
```

- [ ] **Step 3: Script anti-flash em `frontend/index.html`**

Adicionar dentro do `<head>`, após `<title>`:

```html
<script>
  try {
    var t = localStorage.getItem("theme");
    if (t !== "light" && t !== "dark")
      t = matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
    document.documentElement.dataset.theme = t;
  } catch (e) {
    document.documentElement.dataset.theme = "light";
  }
</script>
```

- [ ] **Step 4: Verificar build e testes**

Run: `npm run build && npm test`
Expected: build passa; todos os testes passam.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/theme/ThemeContext.tsx frontend/src/main.tsx frontend/index.html
git commit -m "feat(ui): add ThemeProvider with system tracking and anti-flash script"
```

---

### Task 4: Botão de tema na sidebar

**Files:**
- Modify: `frontend/src/components/Layout.tsx`
- Modify: `frontend/src/styles.css`

- [ ] **Step 1: Adicionar o botão em `Layout.tsx`**

Substituir o conteúdo por:

```tsx
import { NavLink, Outlet } from "react-router-dom";

import { useTheme } from "../theme/ThemeContext";

const LINKS = [
  ["/", "📊", "Dashboard"],
  ["/transacoes", "💳", "Transações"],
  ["/orcamento", "🎯", "Orçamento"],
  ["/importar", "📥", "Importar"],
  ["/config", "⚙️", "Configurações"],
] as const;

const MODE_LABELS = {
  system: ["🖥️", "Tema: sistema"],
  light: ["☀️", "Tema: claro"],
  dark: ["🌙", "Tema: escuro"],
} as const;

export default function Layout() {
  const { mode, cycle } = useTheme();
  const [icon, label] = MODE_LABELS[mode];
  return (
    <div className="layout">
      <aside className="sidebar">
        <h1>Finanças</h1>
        <nav>
          {LINKS.map(([to, icon, label]) => (
            <NavLink key={to} to={to} end={to === "/"}>
              {icon} {label}
            </NavLink>
          ))}
        </nav>
        <button className="theme-toggle" onClick={cycle} title="Alternar tema">
          {icon} {label}
        </button>
      </aside>
      <main className="content">
        <Outlet />
      </main>
    </div>
  );
}
```

- [ ] **Step 2: Estilo do botão no rodapé da sidebar**

Em `frontend/src/styles.css`, trocar o bloco `.sidebar` para layout em coluna e adicionar o estilo do botão (após o bloco `.sidebar a.active`):

```css
.sidebar {
  width: 210px;
  padding: 16px;
  border-right: 1px solid var(--grid);
  background: var(--surface);
  display: flex;
  flex-direction: column;
}
.sidebar nav { flex: 1; }
.theme-toggle {
  margin-top: 12px;
  width: 100%;
  text-align: left;
  font-size: 13px;
  color: var(--ink-2);
  background: none;
  border: 1px solid transparent;
}
.theme-toggle:hover { border-color: var(--border); }
```

Manter as regras existentes de `.sidebar h1`, `.sidebar nav` (`display: flex; flex-direction: column; gap: 2px;`) e `.sidebar a` — apenas acrescentar `flex: 1` ao nav (pode ser na própria regra existente: `.sidebar nav { display: flex; flex-direction: column; gap: 2px; flex: 1; }`).

- [ ] **Step 3: Verificar build**

Run: `npm run build`
Expected: passa.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/Layout.tsx frontend/src/styles.css
git commit -m "feat(ui): add theme toggle button to sidebar"
```

---

### Task 5: Gráficos e LlmFeed theme-aware

**Files:**
- Modify: `frontend/src/components/dashboard/BridgeChart.tsx`
- Modify: `frontend/src/components/dashboard/EvolutionChart.tsx`
- Modify: `frontend/src/components/dashboard/LlmFeed.tsx`

- [ ] **Step 1: `BridgeChart.tsx` usa `useThemeColors`**

Adicionar o import e usar o hook. Diff conceitual — remover a constante `BAR_COLORS` do topo e as cores hardcoded:

```tsx
import { useThemeColors } from "../../theme/ThemeContext";
```

Dentro de `BridgeChart`, após `const [period, setPeriod] = ...`:

```tsx
const colors = useThemeColors();
const barColors = { total: colors.muted, up: colors.blue, down: colors.red } as const;
```

Substituições no JSX:
- `tick={{ fontSize: 11, fill: "#898781" }}` → `tick={{ fontSize: 11, fill: colors.muted }}` (nos dois eixos)
- `axisLine={{ stroke: "#c3c2b7" }}` → `axisLine={{ stroke: colors.baseline }}`
- `<ReferenceLine y={0} stroke="#c3c2b7" />` → `<ReferenceLine y={0} stroke={colors.baseline} />`
- `fill={BAR_COLORS[bar.kind]}` → `fill={barColors[bar.kind]}` (nos dois `map`s de `Cell`)

Remover a linha `const BAR_COLORS = { total: "#898781", up: "#2a78d6", down: "#e34948" } as const;`.

- [ ] **Step 2: `EvolutionChart.tsx` usa `useThemeColors`**

```tsx
import { useThemeColors } from "../../theme/ThemeContext";
```

Dentro de `EvolutionChart`, primeira linha do corpo:

```tsx
const colors = useThemeColors();
```

Substituições:
- `tick={{ fontSize: 11, fill: "#898781" }}` → `tick={{ fontSize: 11, fill: colors.muted }}`
- `axisLine={{ stroke: "#c3c2b7" }}` → `axisLine={{ stroke: colors.baseline }}`
- `fill={i === data.length - 1 ? "#1c5cab" : "#2a78d6"}` → `fill={i === data.length - 1 ? colors.blueDark : colors.blue}`

- [ ] **Step 3: `LlmFeed.tsx` usa a classe `.warn`**

Trocar a linha 32:

```tsx
<div className="card" style={{ borderColor: "#e8b8a8", background: "#fffaf7" }}>
```

por:

```tsx
<div className="card warn">
```

- [ ] **Step 4: Verificar que não sobrou cor hardcoded**

Run: `grep -rn '#[0-9a-fA-F]\{6\}' frontend/src --include='*.tsx'`
Expected: nenhuma ocorrência.

Run: `npm run build && npm test`
Expected: passa.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/dashboard/BridgeChart.tsx frontend/src/components/dashboard/EvolutionChart.tsx frontend/src/components/dashboard/LlmFeed.tsx
git commit -m "feat(ui): make charts and LLM feed theme-aware"
```

---

### Task 6: Verificação visual

**Files:** nenhum (verificação).

- [ ] **Step 1: Subir a stack e capturar screenshots**

Usar a skill `webapp-testing` (Playwright) para abrir o app (`./run.sh` na raiz do projeto, ou `npm run dev` em `frontend/` com o backend rodando) e capturar cada tela (Dashboard, Transações, Orçamento, Importar, Configurações) nos dois temas — clicando no botão da sidebar para alternar.

- [ ] **Step 2: Conferir critérios**

- Texto legível em todas as telas no escuro (sem cinza-sobre-cinza).
- Gráficos com eixos/barras visíveis no escuro.
- Card de alerta do LlmFeed com contraste adequado.
- Recarregar a página mantém o tema escolhido (persistência) e não pisca tema errado no load.
- Modo `system` acompanha `page.emulateMedia({ colorScheme: "dark" })`.

- [ ] **Step 3: Corrigir contrastes se necessário e commitar ajustes**

```bash
git add -A frontend/src
git commit -m "fix(ui): dark palette contrast adjustments"
```

(Somente se houver ajustes.)
